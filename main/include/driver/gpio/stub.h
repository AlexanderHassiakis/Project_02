#pragma once

#include <cstdint>
#include <cstdio>

#include "driver/gpio.h"
#include "driver/gpio/interface.h"

namespace include::driver::gpio
    
    class Interface
    {
    public:
    /**
     * @brief Destructor.
     */
    ~Stub() noexcept = default;
    }

    /**
     * @brief Construct a new Stub object.
     * 
     */

    Stub() noexcept = default;

    /**
     * @brief Turn output the GPIO_pin
     * 
     * @param [in] GPIO pinnumber.
     */

